MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/code_migration?retryWrites=true&w=majority
MONGODB_COLLECTION=Code_Migration
PORT=4000

# Optional — only needed for "Sign in with GitHub" (device flow), which lets you
# clone PRIVATE repos. Register a GitHub OAuth App (Settings > Developer settings
# > OAuth Apps), enable "Device Flow" on it, and put its Client ID here.
# Public-repo cloning works without this.
GITHUB_OAUTH_CLIENT_ID=Ov23liaegRIqbAPSgDV4
